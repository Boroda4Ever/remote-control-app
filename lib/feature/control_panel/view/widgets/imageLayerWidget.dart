import 'package:flutter/material.dart';

class ImageLayer extends StatelessWidget {
  final Image imageData;
  const ImageLayer({super.key, required this.imageData});

  @override
  Widget build(BuildContext context) {
    return imageData;
  }
}
